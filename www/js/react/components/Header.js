var Header = React.createClass({
  render: function () {
    return (
      <header>
        Header here
      </header>
    );
  }
});

ReactDOM.render(
  <Header />,
  document.getElementById('header')
);
